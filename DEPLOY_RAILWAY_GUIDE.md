# 🚀 Пошаговый гайд по деплою на Railway

## 📋 Подготовка

### 1. Регистрация на Railway
1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "Start a New Project"
3. Авторизуйтесь через GitHub

### 2. Создание проекта
1. Выберите "Deploy from GitHub repo"
2. Найдите и выберите `barkoszloy-afk/telegram-bot-project-1`
3. Нажмите "Deploy Now"

## ⚙️ Настройка переменных окружения

В Railway Dashboard → Settings → Environment добавьте:

```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
ADMIN_ID=ваш_telegram_id
CHANNEL_ID=id_вашего_канала (например: -1002510932658)
PORT=8000
```

### Как получить переменные:

**BOT_TOKEN:**
1. Напишите @BotFather в Telegram
2. Используйте `/newbot` для создания нового бота
3. Скопируйте токен

**ADMIN_ID:**
1. Напишите @userinfobot в Telegram
2. Скопируйте ваш ID

**CHANNEL_ID:**
1. Добавьте бота в ваш канал как администратора
2. Отправьте сообщение в канал
3. Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите ID канала (начинается с `-100`)

## 🔧 Настройка деплоя

### 1. Проверка конфигурации
Railway автоматически:
- Обнаружит Python проект
- Установит зависимости из `requirements.txt`
- Запустит команду из `railway.json`: `python3 main_bot_railway.py`

### 2. Мониторинг
После деплоя доступны эндпоинты:
- `/health` - статус бота
- `/logs` - последние логи
- `/` - информация о проекте

## ✅ Проверка работы

1. **Проверьте логи в Railway Dashboard**
   - Перейдите в Deployments → Latest
   - Проверьте логи на ошибки

2. **Протестируйте бота**
   - Найдите вашего бота в Telegram
   - Отправьте `/start`
   - Проверьте, что меню работает

3. **Проверьте healthcheck**
   - Откройте `https://ваш-домен.railway.app/health`
   - Должен вернуться JSON с статусом "healthy"

## 🔄 Автодеплой

При каждом push в `main` ветку:
1. Запускается GitHub Actions CI/CD
2. Если тесты проходят успешно
3. Railway автоматически делает новый деплой

## 🛠️ Отладка проблем

### Если бот не отвечает:
1. Проверьте переменные окружения
2. Проверьте логи в Railway
3. Убедитесь, что BOT_TOKEN правильный
4. Проверьте, что бот добавлен в канал

### Если деплой падает:
1. Проверьте `requirements.txt` на корректность
2. Проверьте синтаксис Python кода
3. Проверьте логи CI/CD в GitHub Actions

## 📊 Полезные команды

```bash
# Локальная проверка перед деплоем
python main_bot_railway.py

# Проверка зависимостей
pip install -r requirements.txt

# Запуск тестов
pytest tests/
```

## 🎯 Готово!

Ваш бот теперь работает в облаке Railway с:
- ✅ Автоматическим деплоем
- ✅ Мониторингом здоровья
- ✅ Логированием
- ✅ CI/CD пайплайном
- ✅ Современной архитектурой

**URL вашего бота:** `https://ваш-проект.railway.app`
